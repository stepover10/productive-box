name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ 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 }}
          GIST_ID: ${{ stepover10 }}
          TIMEZONE: Asia/Seoul
